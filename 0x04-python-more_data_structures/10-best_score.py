#!/usr/bin/python3

def best_score(a_dict):
    if a_dict is None or a_dict == {}:
        return None
    maxval = max(my_dict.values())
    for key, value in my_dict.items():
        if value is biggest:
            return key
